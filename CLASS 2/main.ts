
// FUNCTIONS ON ARRAYS

// let pets = ["dog","cat","rabbit","cow","parrot","horse"];

// pets.pop(); // returns last element of the array.
// pets.push("goat"); //inserts element at the last of the array.

// console.log(pets);

// pets.shift(); // removes first element of array and move other elements one index down. POP BUT FROM START.
// pets.shift(); // removes first element of array and move other elements one index down.
// pets.shift(); // removes first element of array and move other elements one index down.

// pets.unshift("sheep"); // adds element at the start in the array. PUSH BUT FROM START

//array main kahein bhi element ko add kr skte hain.
// pets.splice(3,2,"pigeon"); //'SPLICE(konsi position pr add krna, us index k baad kitni values delete krni, konsi value add krni hai)'

// SLICE: array ko agr torrna ho humne, original array kko nh change krta hai instead array ki copy le kr aik naya array banayega then slice.
// let slicedPets = pets.slice(2,3); // sliced array is stored in it. SLICE(start index konsa hai, ending index knsa hoga)

// console.log(pets);


// FUNCTIONS IN TYPESCRIPT

// function greetUser(firstName:string) {
//     console.log(`Hello ${firstName}, welcome to my first typescript function`);
// }

// greetUser("Sameed");
// greetUser("Hamzah");
